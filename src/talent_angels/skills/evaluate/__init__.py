"""Evaluate (Rank): score routes under a named, versioned policy.

Advanced from "future agent" to skill (ADR-0003). Where a source has no
weights (e.g. ESCO), the policy is our modeling decision — declared and
cited, never presented as source data. Skeleton.
"""
