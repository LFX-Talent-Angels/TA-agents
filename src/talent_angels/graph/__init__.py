"""Knowledge graph layer.

Holds the graph model, ingestion, queries, and Graph-RAG retrieval that the
agents build on. Graph store (e.g. Neo4j) and vector index are decided via ADR
(see TA-workspace/docs/decisions/).

Skeleton — implementation to be added (see ROADMAP deliverables 1, 5, 6).
"""
