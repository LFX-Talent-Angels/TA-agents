"""Connect (Reveal): neighbors, hierarchy, and gaps around a resolved node.

Single-hop traversal over relationship types the suite marks traversable.
Deterministic — one Cypher hop via the suite contract. Skeleton.
"""
